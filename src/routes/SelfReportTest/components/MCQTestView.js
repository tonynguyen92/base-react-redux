import React from 'react'
import PropTypes from 'prop-types'

export const MCQTestView = ({
  testId
}) => {
  return (
    <div>MCQ Test</div>
  )
}

MCQTestView.propTypes = {
  testId  : PropTypes.string
}

export default MCQTestView
